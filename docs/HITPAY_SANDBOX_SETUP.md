# Payment mode

Gateway code is disabled in this independent demo. Checkout uses complete_demo_payment and never calls a live payment provider. No provider secrets are included. Future payment integration requires a separate reviewed implementation and independent credentials.
